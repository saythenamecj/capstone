This is BACKUP System Repository.


!!      Always check commits on Github                                          !!
!!      Before pushing please maglagay kayo commit message para sa iba          !!
!!      Check for errors before commit/push                                     !!
!!      You can practice and make projects in the Practice Templates Folder     !!
!!      Use src folder only for images/assets to use in code                    !!

Commands for Github(in terminal):

    link github Repository
        git remote add origin https://github.com/saythenamecj/capstone.git

    save changes
        git add .

    commit (add context for others)
        git commit -m "First Commit"

    push changes
        git push origin master

    make origin master default push location
        git push --set-upstream origin master


    For you:

    get latest update from repo
        git pull origin master
        git fetch origin master
        git reset --hard origin/master

    save changes
        git add .

    commit (add context for others)
        git commit -m "message"

    push (upload to repo)
        git push

git push --set-upstream origin "name branch"

